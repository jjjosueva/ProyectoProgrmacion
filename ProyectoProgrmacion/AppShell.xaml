<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:ProyectoProgrmacion.Views"
       x:Class="ProyectoProgrmacion.AppShell"
       Title="Sistema de Gestión">

    <FlyoutItem Title="Inicio" Icon="icon_home.png">
        <Tab>
            <ShellContent Title="Home" ContentTemplate="{DataTemplate views:Home}" />
        </Tab>
    </FlyoutItem>

    <FlyoutItem Title="Pedidos" Icon="icon_pedido.png">
        <Tab>
            <ShellContent Title="Detalles de Pedidos" ContentTemplate="{DataTemplate views:DetallesPedidosPage}" />
            <ShellContent Title="Encabezados de Pedidos" ContentTemplate="{DataTemplate views:EncabezadoPedidosPage}" />
        </Tab>
    </FlyoutItem>

    <FlyoutItem Title="Piezas" Icon="icon_pieza.png">
        <Tab>
            <ShellContent Title="Piezas" ContentTemplate="{DataTemplate views:PiezasPage}" />
        </Tab>
    </FlyoutItem>

    <FlyoutItem Title="Pagos" Icon="icon_pago.png">
        <Tab>
            <ShellContent Title="Pagos" ContentTemplate="{DataTemplate views:SistemaPagoPage}" />
        </Tab>
    </FlyoutItem>

</Shell>
